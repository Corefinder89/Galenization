@objects
	hamburger-menu     id      ss-menu
    hamburger-close    css     .close-menu-button

= Hamburger Menu =
    @on mobile
    hamburger-menu:
        height 596px  
        width 250px

= Hmaburger Menu Close =
    @on mobile
    hamburger-close:
        height 50px
        width 50px